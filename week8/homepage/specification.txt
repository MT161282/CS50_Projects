HTML Tags used:
html, head, body, title, h1, h2, h3, p, a, nav, div, ul, ol, li, img, footer, hr

CSS Properties used:
background-color, color, font-size, margin, padding

JavaScript:
Used alert() to show welcome message when page loads.

Bootstrap:
Used navbar for navigation.
